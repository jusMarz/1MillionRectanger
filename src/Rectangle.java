public class Rectangle
{
    private int cornerX;
    private int cornerY;
    private int length;
    private int height;

    public Rectangle(int x, int y, int len, int height)
    {
        cornerX = x;
        cornerY = y;
        length = len;
        this.height = height;
    }

    public Rectangle()
    {
        cornerX = Math.;
        cornerY = y;
        length = len;
        this.height = height;
    }
}
